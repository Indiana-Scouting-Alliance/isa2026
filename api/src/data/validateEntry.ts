import { TeamMatchEntry } from "../utils/dbtypes.ts";

export const validateEntry = (entry: TeamMatchEntry) => {
  let flags: string[] = [];

  //TODO: Add 2026 game-specific validation rules

  return flags;
};
